import React from 'react';
import MainTemplate from "../components/MainTemplate";

const MyReport = () => {
  return (
    <MainTemplate>
      내 독후감
    </MainTemplate>
  );
};

export default MyReport;