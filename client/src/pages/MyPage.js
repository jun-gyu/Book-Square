import React from 'react';
import MainTemplate from "../components/MainTemplate";

const MyPage = () => {
  return (
    <MainTemplate>
      마이 페이지
    </MainTemplate>
  );
};

export default MyPage;